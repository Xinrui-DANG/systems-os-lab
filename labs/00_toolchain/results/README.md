# Results

This directory stores generated machine profiles and perf probe outputs.

Typical files:

```text
machine_profile_YYYY-MM-DD_HH-MM-SS.txt
perf_probe_YYYY-MM-DD_HH-MM-SS.txt
```

These files describe the measurement environment used by later labs.

Only representative profiles should be committed. Large or repeated raw outputs should be ignored.
