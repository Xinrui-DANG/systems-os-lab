# Tools

Repository-level helper tools live here.

Current tools:

| Tool | Purpose |
|---|---|
| `collect_sysinfo.sh` | collect machine profile information |
| `plot_csv.py` | placeholder for plotting CSV experiment results |
| `run_bench.sh` | small command wrapper for benchmark runs |

Lab-local scripts should stay inside each lab folder.
